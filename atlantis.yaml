version: 2
automerge: true
projects:
- name: my-project-name
  dir: .
  workspace: default
  autoplan:
    when_modified: ["*.tf"]
    enabled: true
  apply_requirements: [mergeable, approved]
  workflow: myworkflow
workflows:
  myworkflow:
    plan:
      steps:
      - var: 
        - foo: bar
      - run: echo "running main.tf"
      - plan
    apply:
      steps:
      - run: echo hi
      - var: 
        - foo: bar
      - apply
